export interface ILocation {
  city: string;
  street: string;
  postal_code: string;
}

export interface ISport {
  uuid: string;
  name: string;
}

export interface ISportInput {
  id: string;
  label: string;
}
